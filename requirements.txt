# ---- Core agent runtime ----
langchain-core>=1.2,<2
langgraph>=1.0,<2

# Optional but common LangChain integrations
langchain-community>=0.4,<1

# ---- LLM providers (install now, configure later) ----
langchain-openai>=0.3,<1
langchain-anthropic>=0.3,<1

# ---- Observability / tracing ----
langfuse>=2.40
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27

# ---- Vector DB ----
weaviate-client>=4.7

# ---- Infra clients ----
psycopg[binary]>=3.2
redis>=5.0

# ---- Utilities ----
python-dotenv>=1.0
pydantic>=2.8
rich>=13.8

requests>=2.31